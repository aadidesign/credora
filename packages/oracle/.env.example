# Credora Oracle Service
# Copy to .env and configure

# RPC URL for the target chain
RPC_URL=http://127.0.0.1:8545

# Oracle private key (must be in authorizedOracles)
ORACLE_PRIVATE_KEY=0xac0974bec39a17e36ba4a6b4d238ff944bacb478cbed5efcae784d7bf4f2ff80

# Contract addresses (from deployment)
SCORE_SBT_ADDRESS=0x...
SCORE_ORACLE_ADDRESS=0x...
MOCK_DATA_PROVIDER_ADDRESS=0x...

# Optional: Data provider for scoring (MockDataProvider or custom)
# If using MockDataProvider, ensure it has data for users before running
DATA_PROVIDER_ADDRESS=0x...

# Update interval (seconds) - how often to check for score updates
UPDATE_INTERVAL=300
