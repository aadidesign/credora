# =============================================================================
# Credora Frontend – Environment Variables
# =============================================================================
# Copy to .env.local and configure. All NEXT_PUBLIC_* vars are exposed to the browser.
# =============================================================================

# --- Subgraph (The Graph) ---
# Deploy to Subgraph Studio, then set. api.thegraph.com was sunset June 2024.
# Leave unset for local dev (score history will be empty).
# NEXT_PUBLIC_SUBGRAPH_URL=https://api.studio.thegraph.com/query/<id>/credora/version/latest

# --- Chain ---
# 421614 = Arbitrum Sepolia, 42161 = Arbitrum One, 11155111 = Sepolia, 31337 = Local (Anvil)
NEXT_PUBLIC_CHAIN_ID=421614

# --- App URL ---
# For OpenGraph, canonical URLs. Set in production.
# NEXT_PUBLIC_APP_URL=https://credora.example.com

# --- WalletConnect ---
# Required for WalletConnect QR. Get free Project ID: https://cloud.walletconnect.com
# Without this, only MetaMask/injected wallets work.
# NEXT_PUBLIC_WALLETCONNECT_PROJECT_ID=

# --- Contract Addresses (optional) ---
# Set after deploying. Uses SDK defaults if unset.
# NEXT_PUBLIC_SCORE_SBT_ADDRESS=
# NEXT_PUBLIC_SCORE_ORACLE_ADDRESS=
# NEXT_PUBLIC_PERMISSION_MANAGER_ADDRESS=

# --- Sentry (optional) ---
# Error monitoring. Get DSN from sentry.io
# NEXT_PUBLIC_SENTRY_DSN=
# SENTRY_ORG=your-org
# SENTRY_PROJECT=credora-web
